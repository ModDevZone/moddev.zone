# The source code for the Mod Dev Zone website!

To get started once you have cloned this repo, open it up in your IDE of choice and run ``npm install`` and let that finish.

Once setup is complete you can use ``npm run dev`` in the same terminal window to run a local copy of the site to test with.
